# 修改记录

## 2026-03-28: 设备身份六段码导出/导入

**文件**: `store/transfer.go`, `store/transfer_test.go`

**修改内容**:
新增 `ExportTransferCode()` 和 `ImportTransferCode()` 函数，将已配对设备的身份序列化为六段码格式的字符串，可在另一套系统中导入后直接连接，无需重新扫码配对。

1. `ExportTransferCode(device *Device) (string, error)` — 将设备密钥、ADV、JID 等序列化为二进制，附加 CRC32 校验，base64url 编码后分为 6 段（以 `.` 分隔）
2. `ImportTransferCode(code string) (*Device, error)` — 反序列化六段码，校验 CRC32，恢复完整的 `Device` 结构体（含公钥自动推导）

**二进制格式 (version 1)**:
```
[1B 版本][32B NoiseKey][32B IdentityKey][32B PreKey][4B KeyID][64B PreKeySig]
[4B RegID][32B AdvSecret][32B AdvSigKey][64B AdvAcctSig][64B AdvDevSig]
[2B+N AdvDetails][2B+N JID][2B+N LID][2B+N Platform][16B UUID][8B Timestamp]
[4B CRC32]
```

**用法**:
```go
// 系统A：导出
device, _ := container.GetFirstDevice(ctx)
code, _ := store.ExportTransferCode(device)
fmt.Println(code) // 六段码，以 . 分隔

// 系统B：导入
restored, _ := store.ImportTransferCode(code)
container.PutDevice(ctx, restored)
client := whatsmeow.NewClient(restored, nil)
client.Connect()
```

**限制**:
- 仅导出设备身份（密钥+ADV+JID），不含 Signal 会话、SenderKey、AppState 等
- 导入后需重新建立 E2E 会话（首次向联系人发消息时自动触发）
- 两套系统不可同时使用同一设备身份连接
- 六段码包含私钥材料，需妥善保管

---

## 2026-03-16: 支持自定义配对码

**文件**: `pair-code.go`

**修改内容**:
`GeneratePairCode()` 支持传入可选的自定义配对码参数。

1. 新增 `ErrInvalidPairCode` 错误变量
2. 新增 `normalizePairCode()` 辅助函数 — 去除连字符并验证必须为 8 位纯数字
3. `generateCompanionEphemeralKey` 增加 `customCode string` 参数，非空时使用指定码而非随机生成
4. `GeneratePairCode()` 改为可变参数 `GeneratePairCode(customCode ...string)`，空 = 随机

**用法**:
```go
// 随机码
code, err := cli.GeneratePairCode()

// 自定义码（两种格式均可）
code, err := cli.GeneratePairCode("7777-8888")
code, err := cli.GeneratePairCode("77778888")
```

---

## 2026-02-26: 拆分配对码流程为生成和发送两步

---

## 2026-02-25: 支持发送 InteractiveMessage

**文件**: `send.go`

**问题背景**:
upstream 的 `send.go` 没有对 `InteractiveMessage` 做任何发送端处理（仅处理了 `InteractiveResponseMessage`），导致发送交互式消息时：
- `type` 属性被错误归类为 `"text"`（实际应为 `"media"`）
- 不会生成 `biz` 节点（服务器期望交互式消息带有此节点）
- `mediatype` 属性缺失（交互式消息头部的媒体未被识别）

**修改内容**:

1. `getMediaTypeFromMessage` — 添加 `InteractiveMessage` 的媒体类型识别：
```go
case msg.InteractiveMessage != nil:
    h := msg.InteractiveMessage.GetHeader()
    switch {
    case h.GetImageMessage() != nil:
        return "image"
    case h.GetVideoMessage() != nil:
        return "video"
    case h.GetDocumentMessage() != nil:
        return "document"
    default:
        return ""
    }
```

2. `getButtonTypeFromMessage` — 添加 `InteractiveMessage` → 返回 `"interactive"`，使发送时生成 `biz` 节点：
```go
case msg.InteractiveMessage != nil:
    return "interactive"
```

3. `getButtonAttributes` — 添加 `InteractiveMessage` → 返回空 `Attrs`：
```go
case msg.InteractiveMessage != nil:
    return waBinary.Attrs{}
```

**效果**: 发送 `InteractiveMessage`（含 `nativeFlowMessage` 按钮、CTA URL 等）时，消息节点结构与 WhatsApp Web 一致。

**注意**: 商业账户发送此类交互式消息仍可能被服务器拒绝，因为 WhatsApp 服务端对商业账户有更严格的校验（期望通过官方 Business API 通道发送）。

---

**文件**: `pair-code.go`, `client.go`

**修改内容**:
将 `PairPhone` 方法拆分为两个独立步骤，允许先生成配对码展示给用户，再发送到服务器注册。

1. 新增 `pendingPairCode` 结构体，暂存生成的密钥和配对码
2. 新增 `GeneratePairCode()` 方法 — 纯本地操作，生成临时密钥对和配对码，无网络调用
3. 新增 `SendPairCode()` 方法 — 将之前生成的配对码发送到 WhatsApp 服务器注册
4. `PairPhone()` 保留为便捷方法，内部依次调用 `GeneratePairCode()` + `SendPairCode()`

**用法**:
```go
// 方式一：分步调用
code, err := cli.GeneratePairCode()  // 纯本地，立即返回配对码
// ... 展示 code 给用户 ...
err = cli.SendPairCode(ctx, phone, true, whatsmeow.PairClientChrome, "Chrome (Windows)")

// 方式二：一步调用（向后兼容）
code, err := cli.PairPhone(ctx, phone, true, whatsmeow.PairClientChrome, "Chrome (Windows)")
```

**注意**: 配对码有效期约 160 秒（从 WebSocket 连接建立开始计时），生成后不应等待太久再发送。

---

## 2026-01-22: 添加群成员时附加 Privacy Token

**文件**: `group.go`

**问题背景**:
使用 whatsmeow 原有的 `UpdateGroupParticipants` 方法添加群成员时容易被封号，原因是请求结构与 WhatsApp Web 不一致。

**修改内容**:
1. 添加 `PrivacyTokenExpireDuration` 常量（7天过期时间）
2. 添加 `IsPrivacyTokenExpired()` 函数检查 Token 是否过期
3. 修改 `UpdateGroupParticipants` 方法，在添加群成员时：
   - 获取参与者的 Privacy Token
   - 检查 Token 是否过期
   - 仅在 Token 有效时附加到请求中

**关键改动** (第 24-30 行新增):
```go
const PrivacyTokenExpireDuration = 7 * 24 * time.Hour

func IsPrivacyTokenExpired(timestamp time.Time) bool {
    return time.Since(timestamp) > PrivacyTokenExpireDuration
}
```

**UpdateGroupParticipants 新增逻辑** (第 204-220 行):
```go
// Attach valid privacy token when adding participants (mimics WhatsApp Web behavior)
if action == ParticipantChangeAdd {
    pt, err := cli.Store.PrivacyTokens.GetPrivacyToken(ctx, participantJID)
    if err != nil {
        cli.Log.Warnf("Failed to get privacy token for %s: %v", participantJID, err)
    } else if pt != nil {
        if !IsPrivacyTokenExpired(pt.Timestamp) {
            content[i].Content = []waBinary.Node{{
                Tag:     "privacy",
                Content: pt.Token,
            }}
        } else {
            cli.Log.Debugf("Privacy token expired for %s", participantJID)
        }
    }
}
```

**效果**: 模拟 WhatsApp Web 的行为，降低被封号风险。

---

## 2026-01-20: 添加 SkipOwnDevices 选项

**文件**: `send.go`, `internals.go`

**修改内容**:
- 在 `SendRequestExtra` 中添加 `SkipOwnDevices` 字段
- 设为 `true` 时，发送的消息不会同步到自己的其他设备

**用法**:
```go
cli.SendMessage(ctx, targetJID, message, whatsmeow.SendRequestExtra{
    SkipOwnDevices: true,
})
```

**注意**: 仅对私聊消息有效，群组消息使用 sender key 加密机制，无法跳过同步。

---

## 2026-01-20: 配对码改为纯数字格式

**文件**: `pair-code.go`

**修改内容**:
- 将配对码从 base32 编码（字母+数字混合）改为纯数字形式
- 之前格式: `ABCD-EFGH`（使用字符集 `123456789ABCDEFGHJKLMNPQRSTVWXYZ`）
- 现在格式: `1234-5678`（8位纯数字，范围 00000000-99999999）

**具体改动**:
1. 移除了 `encoding/base32` import
2. 移除了 `linkingBase32` 变量
3. 将 5 字节随机数转换为 8 位纯数字码（通过取模 100000000）
4. 返回格式保持 `XXXX-XXXX`，但现在全是数字
